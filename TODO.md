# TODO

- `--per-page` window height calculation does not take into account modern Intel macs that run Big Sur or Monterey (yet only Catalina and older OSes). This would result in a smaller window than required, thus forcing the user to scroll (unless they specify the `--height` option manually).
